// Shared Pagefind CLI options used by both the dev helper and the production
// search build script.

export const PAGEFIND_ROOT_SELECTOR = ".archive";
export const PAGEFIND_EXCLUDE_SELECTORS = ".line-numbers-wrapper,.version";
